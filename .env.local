# Browser-side: calls go to same-origin Next.js proxy → avoids cross-origin cookie issues
NEXT_PUBLIC_API_URL=/api

# Server-side proxy destination (used by next.config.ts rewrites)
BACKEND_URL=http://10.1.161652.1652145:502012550
